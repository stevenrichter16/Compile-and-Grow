using UnityEngine;

/// <summary>
/// Marker component that identifies a GameObject as an interactable terminal.
/// PlayerController scans for these to enable proximity-based terminal access.
/// </summary>
public sealed class TerminalInteractable : MonoBehaviour { }
